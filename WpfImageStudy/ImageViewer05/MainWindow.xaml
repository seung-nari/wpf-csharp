<Window x:Class="ImageViewer05.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ImageViewer05 - Step5 (GeoTIFF basics)"
        Width="1200" Height="780"
        Background="#222">
    <DockPanel>

        <!-- Top -->
        <Border DockPanel.Dock="Top" Background="#F2F2F2" Padding="10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="이미지 열기 (GeoTIFF 포함)" Width="190" Height="32"
                        Click="BtnOpen_Click"/>
                <Button Content="초기화(1:1)" Width="110" Height="32" Margin="8,0,0,0"
                        Click="BtnReset_Click"/>

                <TextBlock x:Name="TxtPath" Margin="12,0,0,0" VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis" Width="680"/>
            </StackPanel>
        </Border>

        <!-- Bottom status -->
        <Border DockPanel.Dock="Bottom" Background="#111" Padding="8">
            <TextBlock x:Name="TxtStatus" Foreground="#EAEAEA"
                       FontFamily="Consolas" FontSize="12"
                       Text="Wheel: Zoom / Drag: Pan / Move: Pixel &amp; Geo coord" />
        </Border>

        <!-- Viewport -->
        <Border Margin="10" BorderBrush="#555" BorderThickness="1" Background="Black" ClipToBounds="True">
            <Grid x:Name="Viewport"
                  MouseWheel="Viewport_MouseWheel"
                  MouseLeftButtonDown="Viewport_MouseLeftButtonDown"
                  MouseLeftButtonUp="Viewport_MouseLeftButtonUp"
                  MouseMove="Viewport_MouseMove">

                <Image x:Name="ImgMain"
                       Stretch="None"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       SnapsToDevicePixels="True">
                    <Image.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="ScaleTf" ScaleX="1" ScaleY="1"/>
                            <TranslateTransform x:Name="TranslateTf" X="0" Y="0"/>
                        </TransformGroup>
                    </Image.RenderTransform>
                </Image>

            </Grid>
        </Border>

    </DockPanel>
</Window>
